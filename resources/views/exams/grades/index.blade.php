<x-app-layout><x-slot name="header"><h2 class="text-xl font-semibold text-slate-900">Grade Master</h2></x-slot><div class="mx-auto max-w-7xl px-4 py-8 sm:px-6 lg:px-8"><form method="POST" action="{{ route('exams.grades.store') }}" class="mb-6 grid grid-cols-1 gap-3 rounded-lg border border-slate-200 bg-white p-4 shadow-sm md:grid-cols-6">@csrf<select name="programme_id" class="rounded-md border-gray-300" required><option value="">Programme</option>@foreach($programmes as $programme)<option value="{{ $programme->programme_id }}">{{ $programme->name }}</option>@endforeach</select><x-text-input name="grade_letter" placeholder="AA" required /><x-text-input name="min_percentage" type="number" step="0.01" placeholder="Min %" required /><x-text-input name="max_percentage" type="number" step="0.01" placeholder="Max %" required /><x-text-input name="grade_point" type="number" step="0.01" placeholder="Point" required /><button class="rounded-lg bg-cyan-700 px-4 py-2 text-sm font-semibold text-white">Save</button><x-text-input name="description" placeholder="Description" class="md:col-span-6" /></form><div class="overflow-hidden rounded-lg border border-slate-200 bg-white shadow-sm"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50"><tr><th class="px-4 py-3 text-left">Programme</th><th class="px-4 py-3 text-left">Grade</th><th class="px-4 py-3 text-left">Range</th><th class="px-4 py-3 text-left">Point</th><th></th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($grades as $grade)<tr><td class="px-4 py-3">{{ $grade->programme?->name }}</td><td class="px-4 py-3">{{ $grade->grade_letter }}</td><td class="px-4 py-3">{{ $grade->min_percentage }} - {{ $grade->max_percentage }}</td><td class="px-4 py-3">{{ $grade->grade_point }}</td><td class="px-4 py-3 text-right"><form method="POST" action="{{ route('exams.grades.destroy', $grade) }}" onsubmit="return confirm('Delete grade?')">@csrf @method('DELETE')<button class="font-semibold text-red-600">Delete</button></form></td></tr>@empty<tr><td colspan="5" class="px-4 py-6 text-center text-slate-500">No grades.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $grades->links() }}</div></div></x-app-layout>
