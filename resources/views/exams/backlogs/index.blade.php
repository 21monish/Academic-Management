<x-app-layout><x-slot name="header"><h2 class="text-xl font-semibold text-slate-900">Backlog Tracking</h2></x-slot><div class="mx-auto max-w-7xl px-4 py-8 sm:px-6 lg:px-8"><form method="POST" action="{{ route('exams.backlogs.store') }}" class="mb-6 grid grid-cols-1 gap-3 rounded-lg border border-slate-200 bg-white p-4 shadow-sm md:grid-cols-4">@csrf<select name="student_id" class="rounded-md border-gray-300" required><option value="">Student</option>@foreach(\App\Models\Student::orderBy('enrollment_no')->get(['student_id','enrollment_no','first_name','last_name']) as $student)<option value="{{ $student->student_id }}">{{ $student->enrollment_no }} - {{ $student->first_name }}</option>@endforeach</select><select name="subject_id" class="rounded-md border-gray-300" required><option value="">Subject</option>@foreach($subjects as $subject)<option value="{{ $subject->subject_id }}">{{ $subject->code }}</option>@endforeach</select><select name="original_exam_id" class="rounded-md border-gray-300" required><option value="">Exam</option>@foreach($exams as $exam)<option value="{{ $exam->exam_id }}">{{ $exam->exam_name }}</option>@endforeach</select><select name="semester_id" class="rounded-md border-gray-300" required><option value="">Semester</option>@foreach($semesters as $semester)<option value="{{ $semester->semester_id }}">Sem {{ $semester->semester_no }}</option>@endforeach</select><select name="backlog_type" class="rounded-md border-gray-300"><option>ATKT</option><option>Regular</option></select><x-text-input name="attempt_number" type="number" value="1" /><select name="status" class="rounded-md border-gray-300"><option>Pending</option><option>Cleared</option><option>Lapsed</option></select><x-text-input name="registered_on" type="date" /><button class="rounded-lg bg-cyan-700 px-4 py-2 text-sm font-semibold text-white md:col-span-4">Save Backlog</button></form><div class="overflow-hidden rounded-lg border border-slate-200 bg-white shadow-sm"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50"><tr><th class="px-4 py-3 text-left">Student</th><th class="px-4 py-3 text-left">Subject</th><th class="px-4 py-3 text-left">Attempt</th><th class="px-4 py-3 text-left">Status</th><th></th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($backlogs as $backlog)<tr><td class="px-4 py-3">{{ $backlog->student?->enrollment_no }}</td><td class="px-4 py-3">{{ $backlog->subject?->code }}</td><td class="px-4 py-3">{{ $backlog->attempt_number }}</td><td class="px-4 py-3">{{ $backlog->status }}</td><td class="px-4 py-3 text-right"><form method="POST" action="{{ route('exams.backlogs.destroy', $backlog) }}" onsubmit="return confirm('Delete backlog?')">@csrf @method('DELETE')<button class="font-semibold text-red-600">Delete</button></form></td></tr>@empty<tr><td colspan="5" class="px-4 py-6 text-center text-slate-500">No backlogs.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $backlogs->links() }}</div></div></x-app-layout>
