<x-app-layout><x-slot name="header"><h2 class="text-xl font-semibold text-slate-900">Student Promotion</h2></x-slot><div class="mx-auto max-w-7xl px-4 py-8 sm:px-6 lg:px-8"><form method="POST" action="{{ route('exams.promotions.store') }}" class="mb-6 grid grid-cols-1 gap-3 rounded-lg border border-slate-200 bg-white p-4 shadow-sm md:grid-cols-4">@csrf<select name="student_id" class="rounded-md border-gray-300" required><option value="">Student</option>@foreach($students as $student)<option value="{{ $student->student_id }}">{{ $student->enrollment_no }} - {{ $student->first_name }}</option>@endforeach</select><select name="from_semester_id" class="rounded-md border-gray-300" required><option value="">From sem</option>@foreach($semesters as $semester)<option value="{{ $semester->semester_id }}">Sem {{ $semester->semester_no }}</option>@endforeach</select><select name="to_semester_id" class="rounded-md border-gray-300" required><option value="">To sem</option>@foreach($semesters as $semester)<option value="{{ $semester->semester_id }}">Sem {{ $semester->semester_no }}</option>@endforeach</select><select name="academic_year_id" class="rounded-md border-gray-300" required><option value="">Year</option>@foreach($academicYears as $year)<option value="{{ $year->academic_year_id }}">{{ $year->label }}</option>@endforeach</select><select name="promotion_status" class="rounded-md border-gray-300"><option>Promoted</option><option>Detained</option><option>Withdrawn</option></select><x-text-input name="backlogs_at_promotion" type="number" value="0" /><x-text-input name="sgpa_at_promotion" type="number" step="0.01" placeholder="SGPA" /><x-text-input name="attendance_pct" type="number" step="0.01" placeholder="Attendance %" /><label class="inline-flex items-center"><input type="hidden" name="is_manual_override" value="0"><input type="checkbox" name="is_manual_override" value="1" class="rounded border-slate-300 text-cyan-700"><span class="ms-2 text-sm">Manual override</span></label><select name="approved_by" class="rounded-md border-gray-300"><option value="">Approved by</option>@foreach($staffMembers as $staff)<option value="{{ $staff->staff_id }}">{{ $staff->first_name }} {{ $staff->last_name }}</option>@endforeach</select><x-text-input name="remarks" placeholder="Remarks" class="md:col-span-2" /><button class="rounded-lg bg-cyan-700 px-4 py-2 text-sm font-semibold text-white md:col-span-4">Save Promotion</button></form><div class="overflow-hidden rounded-lg border border-slate-200 bg-white shadow-sm"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50"><tr><th class="px-4 py-3 text-left">Student</th><th class="px-4 py-3 text-left">From</th><th class="px-4 py-3 text-left">To</th><th class="px-4 py-3 text-left">Status</th><th></th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($promotions as $promotion)<tr><td class="px-4 py-3">{{ $promotion->student?->enrollment_no }}</td><td class="px-4 py-3">Sem {{ $promotion->fromSemester?->semester_no }}</td><td class="px-4 py-3">Sem {{ $promotion->toSemester?->semester_no }}</td><td class="px-4 py-3">{{ $promotion->promotion_status }}</td><td class="px-4 py-3 text-right"><form method="POST" action="{{ route('exams.promotions.destroy', $promotion) }}" onsubmit="return confirm('Delete promotion?')">@csrf @method('DELETE')<button class="font-semibold text-red-600">Delete</button></form></td></tr>@empty<tr><td colspan="5" class="px-4 py-6 text-center text-slate-500">No promotions.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $promotions->links() }}</div></div></x-app-layout>
