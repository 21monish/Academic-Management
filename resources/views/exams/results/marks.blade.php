<x-app-layout><x-slot name="header"><h2 class="text-xl font-semibold text-slate-900">Marks Entry</h2></x-slot><div class="mx-auto max-w-7xl px-4 py-8 sm:px-6 lg:px-8">@include('partials._flash')<form method="GET" class="mb-6 flex gap-3 rounded-lg border border-slate-200 bg-white p-4"><select name="exam_subject_id" class="w-full rounded-md border-gray-300" required><option value="">Select exam subject</option>@foreach($examSubjects as $item)<option value="{{ $item->exam_subject_id }}" @selected(request('exam_subject_id') == $item->exam_subject_id)>{{ $item->exam?->exam_name }} / {{ $item->subject?->code }}</option>@endforeach</select><button class="rounded-lg bg-slate-900 px-4 py-2 text-sm font-semibold text-white">Load</button></form>@if($examSubject)<form method="POST" action="{{ route('exams.marks.store') }}" class="overflow-hidden rounded-lg border border-slate-200 bg-white shadow-sm">@csrf<input type="hidden" name="exam_subject_id" value="{{ $examSubject->exam_subject_id }}"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50"><tr><th class="px-4 py-3 text-left">Student</th><th class="px-4 py-3">Theory</th><th class="px-4 py-3">Practical</th><th class="px-4 py-3">Internal</th><th class="px-4 py-3">Status</th></tr></thead><tbody class="divide-y divide-slate-100">@foreach($students as $student)@php($enrollment = $student->enrollments->first())<tr><td class="px-4 py-3">{{ $student->enrollment_no }} - {{ $student->first_name }} {{ $student->last_name }}<input type="hidden" name="marks[{{ $student->student_id }}][enrollment_id]" value="{{ $enrollment?->enrollment_id }}"></td><td class="px-4 py-3"><x-text-input name="marks[{{ $student->student_id }}][theory_marks]" type="number" step="0.01" /></td><td class="px-4 py-3"><x-text-input name="marks[{{ $student->student_id }}][practical_marks]" type="number" step="0.01" /></td><td class="px-4 py-3"><x-text-input name="marks[{{ $student->student_id }}][internal_marks]" type="number" step="0.01" /></td><td class="px-4 py-3"><select name="marks[{{ $student->student_id }}][result_status]" class="rounded-md border-gray-300"><option value="">Auto</option><option>Pass</option><option>Fail</option><option>ATKT</option><option>Absent</option></select></td></tr>@endforeach</tbody></table><div class="p-4 text-right"><button class="rounded-lg bg-cyan-700 px-4 py-2 text-sm font-semibold text-white">Save Marks</button></div></form>@endif</div></x-app-layout>
